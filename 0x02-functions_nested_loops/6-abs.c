#include "main.h"
/**
 * main - a function that computes the absolute value of an integer
 *
 * Return: Always 0 (Success)
 */
int _abs(int)
{
	if (n > 0)
	{
		_putchar('+');
		return (1);
	}
	else if (n < )
	{ 
		_putchar('-');
		return (-1);
	}
	else
	{
		_putchar('0');
		return (0);
	}
}
