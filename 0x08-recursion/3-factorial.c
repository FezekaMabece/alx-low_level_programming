#include "main.h"

/**
* factorial - a function that returns the factorial of a given number
* @n: will be the given number
*
* Return: Always 0
*/
int factorial(int n)
if(i <= 1)
{
return 1;
}
return i * factorial(i - 1);
{
int  main()
int i = n;
printf("Factorial of %d is %d\n", i, factorial(i));
return 0;
}
